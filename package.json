{
  "name": "hrconnect",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "npm start --prefix backend",
    "test": "npm test --prefix backend"
  }
}
